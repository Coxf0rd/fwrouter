#
# /etc/fwrouter/local.conf
#
# Local DNS overrides used by fwrouter-apply (dnsmasq).
#
# Обычно этот файл создаётся инсталлятором автоматически (по LAN IP).
# Если создаёшь вручную — заполни:
#
# domain=fwrouter.lan
# address=192.168.0.1
# extra_domains=router.lan,ui.lan
