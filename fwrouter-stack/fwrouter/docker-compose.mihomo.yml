services:
  mihomo:
    image: metacubex/mihomo:v1.19.19
    container_name: fwrouter-mihomo
    restart: unless-stopped

    network_mode: host

    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun

    volumes:
      - /etc/fwrouter/mihomo/config.yaml:/root/.config/mihomo/config.yaml:ro
      - /var/lib/fwrouter/mihomo/state:/root/.config/mihomo
      - /var/lib/fwrouter/mihomo:/root/.config/mihomo/providers

    environment:
      - TZ=UTC
