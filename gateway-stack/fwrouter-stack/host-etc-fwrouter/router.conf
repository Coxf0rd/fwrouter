#
# /etc/fwrouter/router.conf
#
# Optional overrides for interfaces and ports.
# If missing, fwrouter scripts use defaults suitable for a common Debian gateway.
#
# Examples:
# lan_if=enp2s0
# wan_if=enp1s0
# tailscale_if=tailscale0
# tailscale_cidr=100.64.0.0/10
# redir_port=7892
# api_port=9092
