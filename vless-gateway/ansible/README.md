# Ansible (optional)

Deploys this folder to a target host and starts `docker compose`.

Copy `inventory.example.ini` to `inventory.ini` and run:

```bash
ansible-playbook -i inventory.ini playbook.yml
```
